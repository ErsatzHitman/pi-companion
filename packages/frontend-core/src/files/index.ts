/**
 * Files domain — plan.md §6/§7.1.
 *
 * Owns file and terminal session controller state shared by the web
 * files/terminal routes and the Android file/terminal screens.
 *
 * This is a Phase 1 (T14) skeleton stub only. Real logic lands with the
 * Phase 2/3 core tasks that build on the connection and sessions
 * domains (see docs/issues-from-plan.md). Do not add file/terminal
 * controller logic here outside of those tasks.
 *
 * Repository invariant: this module must never import React, React
 * Native, Expo, DOM types, or browser globals.
 */
export const FILES_DOMAIN_STUB = true;
